// Injected via vite.config.ts's `define` — build-time id, compared at
// runtime against /version.json to detect a stale service worker (see
// pwa-register.ts's checkBuildVersion).
declare const __APP_BUILD_ID__: string;
